#include <bits/stdc++.h>
#include <math.h>
using namespace std;

int main()
{
    int a,b,c,s;
    float kottuk,area;

    scanf("%d%d%d",&a,&b,&c);

    s = (a+b+c)/2;

    kottuk = s*(s-a)*(s-b)*(s-c);

    area = sqrt(kottuk);

    printf("Area: %f\n", area);

    return 0;
}
