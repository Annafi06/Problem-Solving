#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a,b;
    scanf("%d%d",&a,&b);

    if(a>0 && b>0)
    {
        printf("Quadrant 1\n");
    }
    else if(a<0 && b>0)
    {
        printf("Quadrant 2\n");
    }
    else if(a<0 && b<0)
    {
        printf("Quadrant 3\n");
    }
    else
    {
        printf("Quadrant 4\n");
    }

    return 0;
}
